| references a Null character (ascii code <§>)
| uid is unique to each user so only it is required for further requests to the server
| each client keeps a copy of the current connected clients which is updated when the server sends data


client to server
to connect 			: uid|username
to leave   			: uid|close

client details      {
                        UID:[username,ipaddress]
                    }

client to client
send a message		: uid|message